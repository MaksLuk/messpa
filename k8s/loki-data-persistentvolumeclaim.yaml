apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: freelance
  labels:
    io.kompose.service: loki-data
  name: loki-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
