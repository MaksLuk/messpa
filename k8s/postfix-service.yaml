apiVersion: v1
kind: Service
metadata:
  namespace: freelance
  annotations:
    kompose.cmd: kompose convert --out k8s/
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: postfix
  name: postfix
spec:
  ports:
    - name: "1025"
      port: 1025
      targetPort: 25
  selector:
    io.kompose.service: postfix
